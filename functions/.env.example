# TMDB — https://www.themoviedb.org/settings/api
# Read Access Token (v4 auth). Set for real deploys with:
#   firebase functions:secrets:set TMDB_ACCESS_TOKEN
TMDB_ACCESS_TOKEN=

# Optional: v3 API key, used only if TMDB_ACCESS_TOKEN is unset.
TMDB_API_KEY=
