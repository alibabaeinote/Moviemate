{
  "//": "Emulator config for the integration suite. Rules are not loaded: these tests exercise the functions, which run with Admin privileges and bypass rules anyway. Rule behaviour is covered separately in rules-tests/.",
  "emulators": {
    "firestore": { "port": 8080, "host": "127.0.0.1" },
    "ui": { "enabled": false },
    "singleProjectMode": true
  }
}
