{
  "name": "moviemate-functions",
  "version": "0.1.0",
  "private": true,
  "description": "MovieMate Cloud Functions \u2014 pairing, recommendation engine, TMDB cache, FCM",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "serve": "npm run build && firebase emulators:start --only functions,firestore,auth",
    "deploy": "firebase deploy --only functions",
    "test:integration": "firebase emulators:exec --only firestore --project moviemate-test --config integration/firebase.json 'vitest run --config integration/vitest.config.ts'"
  },
  "dependencies": {
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.3.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5",
    "firebase-functions-test": "^3.4.0",
    "firebase-tools": "^13.29.1"
  }
}
