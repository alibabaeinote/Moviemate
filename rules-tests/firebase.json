{
  "//": "Minimal config so `firebase emulators:exec` knows what to start. The rules under test are loaded directly from ../firestore.rules by the test setup, so this file never needs to know about them.",
  "emulators": {
    "firestore": { "port": 8080, "host": "127.0.0.1" },
    "ui": { "enabled": false },
    "singleProjectMode": true
  }
}
